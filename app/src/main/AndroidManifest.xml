<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.filmder">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Filmder">
        <activity android:name=".filmder.activities.activities.firebase.friends"></activity>
        <activity android:name=".filmder.activities.activities.BasicActivity" />
        <activity android:name=".HomePageActivity" />
        <activity android:name=".filmder.activities.activities.Login" />
        <activity android:name=".filmder.activities.activities.signup" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/title_activity_login" />
        <activity
            android:name=".homePage"
            android:screenOrientation="portrait" />
        <activity
            android:name=".filmder.activities.activities.MainActivity"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>