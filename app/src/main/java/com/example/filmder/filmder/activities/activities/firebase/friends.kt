package com.example.filmder.filmder.activities.activities.firebase

import androidx.appcompat.app.AppCompatActivity
import android.os.Bundle
import android.util.Log
import android.view.ViewGroup
import android.widget.*
import androidx.core.view.updateLayoutParams
import com.example.filmder.R
import java.text.DateFormatSymbols

class friends : AppCompatActivity() {

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_friends)
        /*Log.i("Velicina", "bla15")
        val arrayname= arrayOf(1,2,3,4,5)
        val duzina= arrayname.size
        var i=0
        val finalParent= findViewById<LinearLayout>(R.id.prijateljipozadina) as ViewGroup
        if (finalParent==null)
            Log.i("Velicina", "bla20")
        Log.i("Velicina", "bla21")
        while(i<duzina) {
            val parent= LinearLayout(this)
            parent.layoutParams= LinearLayout.LayoutParams(LinearLayout.LayoutParams.MATCH_PARENT, 60)
            //parent.orientation= LinearLayout.HORIZONTAL
            Log.i("Velicina", "bla25")
            val iv= ImageView(this)
            iv.setImageResource(R.drawable.friends)
            iv.layoutParams=LinearLayout.LayoutParams(50, 50)
            parent.addView(iv)
            Log.i("Velicina", "bla30")

            val tv=TextView(this)
            tv.layoutParams= LinearLayout.LayoutParams(LinearLayout.LayoutParams.WRAP_CONTENT,LinearLayout.LayoutParams.MATCH_PARENT)
            tv.text=arrayname[i].toString()
            parent.addView(tv)
            Log.i("Velicina", "bla36")
            val btn= Button(this)
            btn.layoutParams= LinearLayout.LayoutParams(LinearLayout.LayoutParams.WRAP_CONTENT,LinearLayout.LayoutParams.MATCH_PARENT)
            btn.text="Pair"
            parent.addView(btn)
            Log.i("Velicina", "${i}")
            Log.i("Velicina", "${arrayname.size}")
            i+=1
            Log.i("Velicina", "bla44")

           finalParent.addView(parent)
            Log.i("Velicina", "bla46")
        } */
        





    }


}